// Paper texture is handled via body::before in globals.css — no JS component needed.
// This component is kept as a no-op so layout.tsx import still resolves.
export default function Atmosphere() {
  return null
}
